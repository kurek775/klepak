<% content_for :title, "New record" %>

<div class="max-w-2xl mx-auto mt-10">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">New Record</h1>

  <div class="bg-white rounded-2xl shadow-md p-8">
    <%= render "form", record: @record %>
  </div>

  <div class="mt-6">
    <%= link_to "← Back to records", records_path,
          class: "text-indigo-600 hover:text-indigo-800 font-medium underline" %>
  </div>
</div>
