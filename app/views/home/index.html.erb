<div class="min-h-screen flex items-center justify-center bg-gray-100 px-4">
  <div class="w-full max-w-md bg-white rounded-2xl shadow-2xl shadow-gray-300/50 p-8 text-center">
    <% if user_signed_in? %>
      <h2 class="text-2xl font-semibold text-gray-800 mb-4">
        👋 Hello, <%= current_user.name %>!
      </h2>
    <% else %>
      <h1 class="text-3xl font-bold text-gray-800 mb-2">Welcome</h1>
      <p class="text-gray-500 mb-6">Please sign in to continue</p>

      <%= form_tag("/oauth/google_oauth2", method: "post", data: { turbo: "false" }) do %>
        <button type="submit"
          class="border border-gray-300 hover:border-gray-500 text-black font-semibold px-6 py-2 rounded-lg transition shadow-sm hover:shadow-md">
          Sign in with Google
        </button>
      <% end %>
    <% end %>
  </div>
</div>
